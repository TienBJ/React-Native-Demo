import { View, StyleSheet, Text, Button, TouchableOpacity} from 'react-native';

export const SignUp = () => {
    return (
        <View style={styles.container}>
            
        </View>
    )
}